package com.michaelwoodroof.worldscape.helper

object ManageFiles {

    // @TODO Implement Image Saving
    fun saveWorld(title : String, desc : String, img : String?) : Boolean {
        if (img != null) {
            // Save Img
        } else {
            // Don't Save
        }
        return true
    }

}